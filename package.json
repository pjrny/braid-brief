{
  "name": "braid-brief-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "pages": "wrangler pages deploy public --project-name braid-brief"
  },
  "devDependencies": {
    "wrangler": "^4.34.0"
  }
}
